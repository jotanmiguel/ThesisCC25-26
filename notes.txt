Ideias:
- Na criação das vms, pedir por um range de utilização, se não for disponibilizado, definir default 1 ano. Quando chegar à data, perguntar a quem as criou se quer continuar ou podem ser apagadas.
- Olhas para formas de como armazenar o estado das vms de maneira eficiente. (se é que isso ja é feito com as solucoes)
- criar vários flavors default para escolha, com opção de customize das imagens em termos de software.
- server de production;
- dashboard com todas as vms do utilizador, com todas as vms do professor por cadeira. DESENVOLVER MAIS ESTA IDEIA.
- Editar uma das imagens disponibilizadas e guardar para usar como "template". Exemplo: Professor altera uma das imagens para ter mais software instalado, guarda essa nova imagem, e posteriormente usa-a para criar VMs para os seus alunos com base nessas imagens. (Snapshot Openstack)
- Log?

Notas:
- Promax tem flexibility and scalability issues. Para além disso, é preciso licença.

Pesquisar:
- statistical multiplexing
- Benchmarks: LINPACK, STREAM, IOzone, IPerf, NAS-OMP, and NAS-MPI.
- High Availability?
- how does the openstack/cloudstack integrate with the already established FCUL's auth system?
- Full vs Incremental vs Differencial.

Duvidas:
- Qual seria os requisitos de vCPU, vRAM e Storage (Root disk and Ephemeral disk)? (flavor)
- Quais seriam os ratios de CPU and RAM allocation ratio. (e.g. 10:1 CPU and 1.5:1 RAM | 8 cores gave 80 vCores and 32GB gave 48GB vRAM)
- Quais OS seriam disponibilizados?
- Potenciais motivos de downtime da cloud. Falha energia, sobrecarga;
- É necessário pensar nos backups. De raiz ou integrar com o sistema já implementado? Cloud Services para esta parte caso seja de raiz?
- Queremos manter isto energy efficient? performance efficient? os dois? existem outras prioridades?
- A memoria das VMs é volatile? cada vez que é desligada, perde-se a memoria?
- Vamos integrar isto com uma cloud do tipo AWS, Azure, Google Cloud, etc...?
Trabalho:
- Ler literatura (artigos, papers, blogs, etc...);
- Instalar Software e Hardware necessario para suportar a nossa solucao (2 servidores, ubuntu 22.04, cloudstack, );
	- cloudstack_01: 10.10.5.51 jose:admin123
	- cloudstack_02: 10.10.5.52 joao:admin123

- Software instalado ate agora:
	Apache CloudStack 4.18.2.5 (packages cloudstack-agent and cloudstack-common) — provides the CloudStack hypervisor agent for KVM hosts.
	KVM/QEMU 6.2 — offers full virtualization support for x86 systems (qemu-system-x86, qemu-utils, and related modules).
	Libvirt 8.0.0 — handles VM lifecycle management and communication between CloudStack and the hypervisor through libvirtd.
	NFS 2.6.1 — provides shared network storage (for primary and secondary CloudStack volumes).
	MariaDB/MySQL client libraries — ensure compatibility with the CloudStack management database.
	Networking utilities such as bridge-utils, ebtables, and vlan — used to configure network bridges (cloudbr0, cloudbr1) required for CloudStack’s advanced networking mode.

